spring.application.name=swaggertest


spring.security.user.name=admin
spring.security.user.password=admin


# PostgreSQL Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/estockmarket
spring.datasource.username=postgres
spring.datasource.password=@Super30
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update  
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Show SQL in console (for debugging)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# HikariCP Connection Pooling (optional, default for Spring Boot)
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
